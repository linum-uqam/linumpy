nextflowVersion = '<= 23.10'
singularity{
    autoMounts = true
    enabled = true
}

process {
    publishDir = {"$params.output"}
    scratch = false
    errorStrategy = { task.attempt <= 3 ? 'retry' : 'ignore' }
    maxRetries = 3
    afterScript='sleep 1'
}
